你正在参与一个生产级 AI 数据底座项目，而不是一次性脚本或 Demo。

项目最终目标（不可偏离）
构建一条 稳定、可增量、可幂等、可观测 的商品数据链路，使：

chihiro（业务库）
→ 只读、不可修改、REPLACE 语义、数据不稳定

belle_ai.products（AI 统一入口）
→ 结构稳定、语义清晰、只要这里对，AI / 向量 / Agent 就能长期复用

product_change_log
→ 成为“唯一可信的变更队列”，驱动向量与后续 AI 能力

本项目的设计哲学（必须遵守）
数据正确性 > 实现速度 > 代码数量

不推倒重写，不破坏已有 agent / worker / vector_store

不修改 chihiro 库任何表结构，只读使用

任何高风险操作必须先分析、再执行

一切为“后续 AI / 向量 / Agent 稳定演进”服务

关于阶段 Prompt
接下来你会收到 P-ETL-1 / P-ETL-2 / P-ETL-3

它们是同一个项目的不同阶段

你必须在实现当前阶段时，始终保证：

不破坏下一阶段的可行性

不引入与最终目标冲突的设计

如果阶段 Prompt 的要求与本总纲冲突，以本总纲为准，并先指出冲突再继续