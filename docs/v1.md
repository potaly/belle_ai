We are designing the initial MySQL schema and realistic seed data
for V1 of the "AI Smart Guide Service".

You MUST output:
1) A full DDL script with explicit CREATE TABLE statements.
2) A full seed script with INSERT statements.
NO pseudo code, NO placeholders, NO "...".  
Return complete SQL files.

-------------------------------------------------------
Tables to create (use CREATE TABLE):
-------------------------------------------------------

1) products  -- 商品表
   - id BIGINT PRIMARY KEY AUTO_INCREMENT
   - sku VARCHAR(64) NOT NULL UNIQUE
   - name VARCHAR(255) NOT NULL
   - price DECIMAL(10,2) NOT NULL
   - tags JSON NULL
   - attributes JSON NULL
   - description TEXT NULL
   - image_url VARCHAR(512) NULL
   - created_at DATETIME NOT NULL
   - updated_at DATETIME NOT NULL
   - INDEX idx_products_sku (sku)

2) user_behavior_logs  -- 用户行为日志
   - id BIGINT PRIMARY KEY AUTO_INCREMENT
   - user_id VARCHAR(64) NOT NULL
   - guide_id VARCHAR(64) NOT NULL
   - sku VARCHAR(64) NOT NULL
   - event_type VARCHAR(32) NOT NULL     -- 'browse','enter_buy_page','click_size_chart','favorite','share'
   - stay_seconds INT NOT NULL DEFAULT 0
   - occurred_at DATETIME NOT NULL
   - INDEX idx_ubl_user_sku (user_id, sku, occurred_at)
   - INDEX idx_ubl_event_time (event_type, occurred_at)

3) ai_task_log  -- AI 调用日志
   - task_id VARCHAR(64) PRIMARY KEY
   - guide_id VARCHAR(64) NULL
   - scene_type VARCHAR(32) NOT NULL     -- 'copy','product_analyze','intent'
   - input_data TEXT NOT NULL
   - output_result TEXT NULL
   - model_name VARCHAR(64) NULL
   - latency_ms INT NULL
   - is_adopted TINYINT(1) NOT NULL DEFAULT 0
   - created_at DATETIME NOT NULL
   - INDEX idx_ai_log_scene_time (scene_type, created_at)

4) guides
   - id BIGINT PRIMARY KEY AUTO_INCREMENT
   - guide_id VARCHAR(64) NOT NULL UNIQUE
   - name VARCHAR(64) NOT NULL
   - shop_name VARCHAR(128) NULL
   - level VARCHAR(32) NULL
   - created_at DATETIME NOT NULL

Add meaningful COMMENT in Chinese for each field.

-------------------------------------------------------
Seed data requirements:
-------------------------------------------------------

In `sql/seed_data.sql`, insert:

1) 100+ product rows:
   - Realistic Chinese names (运动鞋、小白鞋、高跟鞋、凉鞋、短靴……)
   - Reasonable prices 200–2000 RMB
   - Realistic tags: ['百搭','舒适','时尚','增高','通勤','轻便']
   - attributes JSON with color/material/scene/season

2) 50+ guide rows

3) 1000+ user_behavior_logs:
   - last 30 days
   - 70% browse events
   - 20% repeated visits
   - 5–10% high-intent events (enter_buy_page or long stay_seconds)
   - stay_seconds: 5–60 typical
   - realistic multi-user, multi-guide pattern

-------------------------------------------------------
Output format:
-------------------------------------------------------

Return TWO separate SQL blocks:

-- schema.sql
<full CREATE TABLE script>

-- seed_data.sql
<full INSERT script>

No truncation, no omitted parts.